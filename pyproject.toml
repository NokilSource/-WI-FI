[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "system-repair"
version = "0.2.0"
description = "Local Windows audit and remediation with explicit actions and mandatory backups"
requires-python = ">=3.12,<3.15"
dependencies = ["PyQt6==6.11.0", "psutil==7.2.2", "pywin32==312; sys_platform == 'win32'"]

[project.gui-scripts]
system-repair = "system_repair.__main__:main"

[dependency-groups]
dev = ["pytest==9.1.1", "pytest-qt==4.5.0", "ruff==0.16.9"]
build = ["pyinstaller==6.22.3"]

[tool.pytest.ini_options]
testpaths = ["tests"]
qt_api = "pyqt6"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]
